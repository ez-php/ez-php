<?php

declare(strict_types=1);

/**
 * Event listener configuration.
 *
 * Maps event class-strings to arrays of listener class-strings.
 * The EventServiceProvider resolves each listener via the container
 * (autowiring supported) and registers it on boot.
 *
 * Example:
 *
 *   App\Events\UserCreated::class => [
 *       App\Listeners\SendWelcomeEmail::class,
 *       App\Listeners\LogUserCreation::class,
 *   ],
 */
return [
    'listeners' => [
        // Add your event → listener mappings here.
    ],
];
